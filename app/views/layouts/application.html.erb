<!DOCTYPE html>
<html>
<head>
  <title>Hermes</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= render 'layouts/session' %>

<div id="logo" class="centered">
	<h2><%= link_to " ~ Hermes ~ ", root_url %></h2>
</div>

<% if (current_user && current_user.authorized) || request.fullpath == '/users/sign_in' || request.fullpath == '/users/sign_up' %>
	<%= render 'layouts/navigation' %>
	<%= yield %>
<% end %>

<% if current_user && !current_user.authorized %>
	<p>Your account is pending approval. Contact @blaha to speed things along.</p>
<% end %>

</body>
</html>
